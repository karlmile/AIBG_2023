#include "board.h"

void Board::listNextMoves(PlayerID turn, Board* outBoards, Move* outMoves, size_t maxOutputs) const
{
}

std::ostream& operator << (std::ostream& o, const Board& b)
{

    return o;
}